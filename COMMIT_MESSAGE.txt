fix: Update to GPT-5 models and fix API parameter compatibility

BREAKING CHANGES:
- Migrated from GPT-4 to GPT-5 models across the platform
- Updated API parameters for GPT-5 compatibility

Changes:
- Fixed OpenAI API parameter compatibility:
  * Changed max_tokens to max_completion_tokens for GPT-5 series models
  * Removed temperature parameter for GPT-5 models (only default value supported)
  * Added requiresMaxCompletionTokens() helper function

- Updated model selections:
  * Main chatbot: gpt-5-mini (was gpt-5.2)
  * Sentiment analysis: gpt-5-nano (unchanged)
  * Support bot: gpt-5-mini (was gpt-4o-mini)

- Removed all GPT-4 references:
  * Updated UI text in hero, features, and pricing components
  * Updated backend comments and documentation
  * Changed "GPT-4 Support" to "GPT-5 Support" in pricing/subscriptions

Files modified:
- server/services/ai.ts: Added helper function, fixed parameter handling
- server/services/sentiment.ts: Updated to use max_completion_tokens
- server/controllers/chat.ts: Updated support bot model and parameters
- client/src/components/landing/*: Updated all GPT-4 references to GPT-5

This fixes the chat response issue where GPT-5 models were rejecting
max_tokens and temperature parameters.

