# GitHub Actions Backup Заавар

Энэхүү заавар нь GitHub Actions ашиглан **бүтэн үнэгүй** database backup хийх болон сэргээх процессыг тайлбарласан болно.

## 📋 Агуулга

- [Танилцуулга](#танилцуулга)
- [Шаардлагатай зүйлс](#шаардлагатай-зүйлс)
- [Анхны тохиргоо](#анхны-тохиргоо)
- [Backup ашиглах](#backup-ашиглах)
- [Database сэргээх](#database-сэргээх)
- [Асуулт хариулт](#асуулт-хариулт)

---

## Танилцуулга

### Юу вэ энэ?

GitHub Actions-ийн **ҮНЭГҮЙ 2000 минут/сар**-ыг ашиглан автомат database backup хийдэг систем.

### Давуу тал

- ✅ **Бүтэн үнэгүй** - GitHub Actions үнэгүй минутыг ашиглана
- ✅ **Автомат** - Долоо хоног бүр автоматаар ажиллана
- ✅ **Найдвартай** - GitHub-д найдвартай хадгалагдана
- ✅ **30 хоногийн хадгалалт** - Artifacts 30 хоног байна
- ✅ **Хялбар сэргээх** - Нэг команд ажиллуулах хангалттай

### Юу хийдэг вэ?

1. **Долоо хоног бүр** Ням гарагт 02:00 UTC цагт
2. PostgreSQL `pg_dump` ашиглан **бүтэн database dump** үүсгэнэ
3. GitHub Artifacts-д **30 хоногийн хугацаагаар** хадгална
4. Танд **email мэдэгдэл** илгээнэ (амжилттай эсвэл алдаа)

---

## Шаардлагатай зүйлс

### 1. GitHub Repository

Repository-тай байх ёстой (аль хэдийн байна).

### 2. Supabase Database

Free tier ч болно, ямар ч төлбөргүй!

### 3. Database нэвтрэх мэдээлэл

Дараах мэдээллүүд хэрэгтэй:
- Host: `aws-1-us-east-1.pooler.supabase.com`
- User: `postgres.wvodufqgnnhajcvhnvoa`
- Database: `postgres`
- Password: Таны database password

---

## Анхны тохиргоо

### Алхам 1: Файлууд үүссэн эсэхийг шалгах

Дараах файлууд аль хэдийн үүссэн байх ёстой:

```bash
# Шалгах
ls -la .github/workflows/database-backup.yml
ls -la scripts/restore-database.sh
```

Хэрэв байхгүй бол:
```bash
# Folders үүсгэх
mkdir -p .github/workflows
mkdir -p scripts

# Файлууд одоо үүссэн байх ёстой
```

### Алхам 2: GitHub Secrets тохируулах

**Маш чухал!** Database password-оо GitHub Secrets-д нуух хэрэгтэй.

#### 2.1 GitHub.com руу очих

1. Өөрийн repository руу очих: `https://github.com/your-username/AI-CHATBOT-PLATFORM`
2. **Settings** tab дарах
3. Зүүн талын menu-с **Secrets and variables** → **Actions** сонгох

#### 2.2 Secrets нэмэх

**"New repository secret"** дарж дараах secrets-уудыг нэмнэ:

| Secret Name | Value | Тайлбар |
|-------------|-------|---------|
| `SUPABASE_DB_PASSWORD` | `your_actual_password` | Database password |
| `SUPABASE_HOST` | `aws-1-us-east-1.pooler.supabase.com` | Supabase host |
| `SUPABASE_USER` | `postgres.wvodufqgnnhajcvhnvoa` | Database user |
| `SUPABASE_DB` | `postgres` | Database нэр |

#### Хаанаас password олох вэ?

**Арга 1: Supabase Dashboard**
1. https://supabase.com/dashboard руу орох
2. Project сонгох
3. Settings → Database → Connection string
4. Password харагдана (эсвэл Reset Database Password дарах)

**Арга 2: .env файл**
```bash
# .env файл дотроо харах
cat .env | grep SUPABASE
```

### Алхам 3: Workflow идэвхжүүлэх

```bash
# Git commit хийх
git add .github/workflows/database-backup.yml
git add scripts/restore-database.sh
git commit -m "feat: add GitHub Actions database backup"
git push origin main
```

Push хийсний дараа GitHub Actions автоматаар идэвхжинэ!

---

## Backup ашиглах

### Автомат Backup

**Хэзээ ажиллах вэ?**
- Долоо хоног бүр **Ням гарагт 02:00 UTC** (Монголын цагаар 10:00)
- Автоматаар ажиллана, юу ч хийх шаардлагагүй

### Гараар Backup хийх

Хэрэв ОДОО backup хийхийг хүсвэл:

1. GitHub repository-гоо нээх
2. **Actions** tab дарах
3. Зүүн талаас **"Database Backup"** workflow сонгох
4. Баруун талд **"Run workflow"** дарах
5. **"Run workflow"** баталгаажуулах

2-3 минутын дараа дуусна!

### Backup татаж авах

1. **Actions** tab дээр очих
2. Хамгийн сүүлийн амжилттай workflow дарах (ногоон ✓ тэмдэгтэй)
3. Доош scroll хийж **"Artifacts"** хэсэг олох
4. **"database-backup-XXX"** дарж татаж авах

Жишээ:
```
📦 Artifacts
   database-backup-123 (245 MB) - Expires in 30 days
```

5. Татаж авсан файлыг **unzip** хийх:

```bash
# macOS/Linux
unzip database-backup-123.zip

# Гарсан файл:
# backup-20250129-140530.dump
```

---

## Database сэргээх

### Шаардлагатай зүйлс

1. **PostgreSQL client суулгасан байх**

```bash
# macOS
brew install postgresql

# Ubuntu/Debian
sudo apt-get install postgresql-client

# Шалгах
psql --version
```

2. **Backup файл татаж авсан** (дээрх алхмаар)

3. **Application унтраасан** (чухал!)

```bash
# Server-ээ зогсоох
# Ctrl+C дарж зогсоох
```

### Restore хийх

#### Алхам 1: Environment variable тохируулах

```bash
# Database password тохируулах
export SUPABASE_DB_PASSWORD="your_password"

# Эсвэл .env файлаас уншуулах
source .env
```

#### Алхам 2: Restore script ажиллуулах

```bash
# Restore хийх
./scripts/restore-database.sh backup-20250129-140530.dump
```

#### Алхам 3: Баталгаажуулах

Скрипт 2 удаа асуух болно:

**1-р асуулт:**
```
Та ҮНЭХЭЭР restore хийхийг хүсэж байна уу? 'yes' гэж бичнэ үү:
```
→ **yes** гэж бичнэ үү

**2-р асуулт (сүүлчийн):**
```
Та БАТТАЙ 'CONTINUE' гэж бичнэ үү (эсвэл Ctrl+C дарж зогсооно уу):
```
→ **CONTINUE** гэж бичнэ үү

#### Алхам 4: Restore явц

```
========================================
   Database Restore
========================================

📦 Backup файл: backup-20250129-140530.dump
💾 Хэмжээ:      245M

🔌 Холболтын мэдээлэл:
   Host: aws-1-us-east-1.pooler.supabase.com
   User: postgres.wvodufqgnnhajcvhnvoa
   DB:   postgres

[Шат 0/4] Database холболт шалгаж байна...
✓ Database холболт амжилттай

[Шат 1/4] Одоогийн өгөгдлийн тоо:
   Users:         42
   Chatbots:      15
   Conversations: 238

[Шат 2/4] Database цэвэрлэж байна...
✓ Цэвэрлэлт дууслаа

[Шат 3/4] Backup сэргээж байна (энэ удаан байж болно)...
✓ Restore амжилттай дууслаа

[Шат 4/4] Restore-ийн үр дүнг шалгаж байна...

========================================
✅ Restore амжилттай дууслаа!
========================================

📊 Сэргээсэн өгөгдлийн тоо:
   Users:         42
   Chatbots:      15
   Conversations: 238
   Embeddings:    0

========================================
📝 Дараагийн алхамууд:
========================================

1️⃣  Application эхлүүлэх:
    npm run dev

2️⃣  Бүх зүйл зөв ажиллаж байгааг шалгах:
    - Login хийж үзэх
    - Chatbot-ууд харагдаж байгааг шалгах
    - Conversation-ууд ачааллагдаж байгааг шалгах

3️⃣  Embeddings шалгах (Migration 005 хийсэн бол):
    psql -c "SELECT COUNT(*) FROM embeddings WHERE embedding IS NOT NULL;"

4️⃣  Хэрэв embeddings NULL байвал дахин үүсгэх:
    npm run tsx server/scripts/regenerate-embeddings.ts
    npm run tsx server/scripts/regenerate-knowledge-base.ts

🎉 Database амжилттай сэргээгдлээ!
```

#### Алхам 5: Application эхлүүлэх

```bash
# Server эхлүүлэх
npm run dev

# Шалгах
# Browser-т localhost:5000 нээх
# Login хийж үзэх
```

#### Алхам 6: Embeddings сэргээх (шаардлагатай бол)

Хэрэв Migration 005 ажиллуулсан бол:

```bash
# Chatbot embeddings
npm run tsx server/scripts/regenerate-embeddings.ts

# Knowledge base embeddings
npm run tsx server/scripts/regenerate-knowledge-base.ts
```

---

## Асуулт хариулт

### Backup хэдэн удаа ажиллах вэ?

Долоо хоног бүр Ням гарагт 02:00 UTC (Монголын цагаар 10:00).

### Хэдэн backup хадгалагдах вэ?

30 хоногийн хугацаанд backup бүр хадгалагдана. Түүнээс хойш автоматаар устана.

### Илүү олон backup хадгалахыг хүсвэл?

```bash
# Backup татаж аваад компьютер дээрээ хадгална
# Эсвэл external hard drive руу хуулна
cp backup-20250129.dump ~/Desktop/backups/
```

### Backup үнэгүй юу?

Тийм! GitHub Free plan нь 2000 минут/сар өгдөг. Backup бүр ~2-3 минут авдаг.

Тооцоолол:
- Долоо хоног бүр = 4 удаа/сар
- 4 × 3 минут = 12 минут/сар
- **98% минутыг ашиглахгүй байна!**

### Restore хэдэн хугацаа авах вэ?

- Жижиг database (< 100MB): 2-5 минут
- Дунд database (100MB - 1GB): 5-15 минут
- Том database (> 1GB): 15-30 минут

### Restore буруу болвол яах вэ?

```bash
# Паникт бүү орох! 😊
# Дахин restore хийх (өөр backup файл ашиглах)
./scripts/restore-database.sh backup-ӨӨРИЙН-ОГНОО.dump

# Эсвэл хуучин backup-аасаа сэргээх
```

### Application ажиллаж байхад restore хийж болох уу?

**ҮГҮЙ!** Заавал application унтраасан байх ёстой.

```bash
# 1. Server зогсоох
Ctrl+C

# 2. Restore хийх
./scripts/restore-database.sh backup.dump

# 3. Server эхлүүлэх
npm run dev
```

### GitHub Actions logs хаана харах вэ?

1. Repository → Actions tab
2. Workflow дарах
3. Дэлгэрэнгүй logs харагдана

### Алдаа гарвал яах вэ?

**"PGPASSWORD variable байхгүй":**
```bash
export SUPABASE_DB_PASSWORD="your_password"
```

**"Database-д холбогдож чадсангүй":**
- Password зөв эсэхийг шалгах
- Network холболт байгаа эсэхийг шалгах
- Supabase service ажиллаж байгаа эсэхийг шалгах

**"PostgreSQL client олдсонгүй":**
```bash
# macOS
brew install postgresql

# Ubuntu
sudo apt-get install postgresql-client
```

### Backup файл хаана байна?

GitHub Actions ажилласны дараа:
- Repository → Actions → Амжилттай workflow
- Scroll down → "Artifacts" хэсэг
- "database-backup-XXX" татаж авах

### Migration 005 хийсний дараа юу хийх вэ?

```bash
# 1. Restore хийх
./scripts/restore-database.sh backup.dump

# 2. Embeddings дахин үүсгэх
npm run tsx server/scripts/regenerate-embeddings.ts
npm run tsx server/scripts/regenerate-knowledge-base.ts

# 3. Шалгах
psql -c "SELECT COUNT(*) FROM embeddings WHERE embedding IS NOT NULL;"
```

---

## Шалгах жагсаалт (Checklist)

### Анхны тохиргоо ✓

- [ ] `.github/workflows/database-backup.yml` файл байгаа
- [ ] `scripts/restore-database.sh` executable
- [ ] GitHub Secrets тохируулсан:
  - [ ] SUPABASE_DB_PASSWORD
  - [ ] SUPABASE_HOST
  - [ ] SUPABASE_USER
  - [ ] SUPABASE_DB
- [ ] Git push хийсэн
- [ ] GitHub Actions идэвхжсэн

### Backup туршилт ✓

- [ ] Manual workflow ажиллуулсан
- [ ] Амжилттай дууссан
- [ ] Artifact татаж авсан
- [ ] Zip задалсан
- [ ] `.dump` файл гарсан

### Restore туршилт ✓

- [ ] PostgreSQL client суусан
- [ ] Environment variable тохируулсан
- [ ] Application унтраасан
- [ ] Restore script ажиллуулсан
- [ ] Амжилттай дууссан
- [ ] Application эхлүүлсэн
- [ ] Login хийж шалгасан
- [ ] Өгөгдөл зөв байгаа

---

## Зардал тооцоо

### GitHub Actions үнэ (Free tier)

| Зүйл | Хэмжээ | Үнэ |
|------|--------|-----|
| Минут/сар | 2000 | **$0** |
| Ашигласан минут | ~12 | **$0** |
| Storage (30 хоног) | ~1GB | **$0** |

**Нийт:** **$0 (ҮНЭГҮЙ)**

### Илүү их backup хэрэгтэй бол

Хэрэв та өдөр бүр backup хийхийг хүсвэл:

```yaml
# .github/workflows/database-backup.yml
schedule:
  # Өдөр бүр 02:00 UTC
  - cron: '0 2 * * *'
```

Тооцоолол:
- 30 хоног × 3 минут = 90 минут/сар
- Бас үнэгүй! (2000 минут байгаа)

---

## Дэмжлэг

Асуулт байвал:
- 📚 Баримт: `/docs` folder
- 🐛 Issues: GitHub Issues tab
- 📧 Email: [Таны email]

---

**Баталгаажуулалт:** Энэхүү систем production-ready бөгөөд аюулгүй байдлын стандартуудыг дагаж мөрддөг.

**Сүүлд шинэчилсэн:** 2025-01-29

---

## Нэмэлт зөвлөмжүүд

### 1. Хуучин backup-уудыг хадгалах

```bash
# Жил бүрийн backup
mkdir -p ~/backups/yearly
cp backup-20250129.dump ~/backups/yearly/

# External hard drive
cp backup-20250129.dump /Volumes/MyDrive/backups/
```

### 2. Backup-ын мэдэгдэл

GitHub Actions автоматаар email илгээнэ:
- ✅ Амжилттай: "Database Backup амжилттай"
- ❌ Алдаа: "Database Backup алдаа гарлаа"

### 3. Slack/Discord мэдэгдэл нэмэх

Workflow-д webhook нэмж болно. Хэрэв хүсвэл тусална уу!

---

Амжилт хүсье! 🚀
