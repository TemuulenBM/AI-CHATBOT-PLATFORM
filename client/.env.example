# ===========================================
# ConvoAI - Client Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values
# For production, set these in Vercel dashboard

# ----- Clerk Authentication (Required) -----
# Get this from: https://dashboard.clerk.com/last-active?path=api-keys
# This is the PUBLISHABLE key (safe to expose in frontend)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY_HERE

# ----- Paddle Payment Gateway (Required) -----
# Get from: Paddle Dashboard > Developer Tools > Authentication > Client-side tokens
# This is a CLIENT TOKEN (safe to expose in frontend)
VITE_PADDLE_CLIENT_TOKEN=test_YOUR_CLIENT_TOKEN_HERE
VITE_PADDLE_ENVIRONMENT=sandbox

# ----- CDN Configuration (Optional) -----
# CDN Provider: vercel (default), cloudinary, cloudflare, imgix
VITE_CDN_PROVIDER=vercel

# Cloudinary Configuration (optional)
# VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Cloudflare Images Configuration (optional)
# VITE_CDN_BASE_URL=https://imagedelivery.net/your-account-hash

# imgix Configuration (optional)
# VITE_CDN_BASE_URL=https://your-source.imgix.net

# ----- Production Checklist -----
# Before deploying to production, ensure:
# [ ] VITE_CLERK_PUBLISHABLE_KEY is your live key (pk_live_...)
# [ ] VITE_PADDLE_CLIENT_TOKEN is your live token
# [ ] VITE_PADDLE_ENVIRONMENT=production
# [ ] CDN provider is configured if using external CDN
